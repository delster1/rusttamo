use ndarray::{arr0, Array0, Array1, Array2};

struct NeuralNet {
	weights_input_hidden : Array2<f32>, // weights connecting input layer to hidden layer
	weights_hidden_output: Array2<f32>, // weights connecgint hidden layer to output layer
	hidden_layer_size : usize,
}

impl NeuralNet {
	
}

fn main() {
	
}